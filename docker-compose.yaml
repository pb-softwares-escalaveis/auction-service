services:
  postgres:
    image: postgres:15-alpine
    container_name: auction-service-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: postgres
    ports:
      - "5435:5432"

volumes:
  oleiloeiro-kafka-data: